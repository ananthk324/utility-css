{
  "name": "utility-css",
  "version": "0.0.1",
  "description": "Utility CSS",
  "main": "index.js",
  "scripts": {
    "compile": "node-sass -w scss/style.scss dist/style.css",
    "compile:min": "node-sass --output-style compressed scss/style.scss dist/minified/style.min.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "utility",
    "css"
  ],
  "author": "AK",
  "license": "MIT",
  "devDependencies": {
    "node-sass": "^5.0.0"
  }
}
